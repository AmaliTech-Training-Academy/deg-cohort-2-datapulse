"""
accounts/exceptions.py
────────────────────────────────────────────────────────────────────────────────
Custom exceptions for the accounts app.

These exceptions are raised in views/serializers and translated to structured
HTTP responses by core.exceptions.custom_exception_handler.

TODO (add exceptions as you implement features):
    Example:
        from rest_framework.exceptions import APIException
        from rest_framework import status

        class InvalidCredentialsError(APIException):
            status_code = status.HTTP_401_UNAUTHORIZED
            default_code = "invalid_credentials"
            default_detail = "Email or password is incorrect."
"""

# Placeholder — no custom exceptions yet.
