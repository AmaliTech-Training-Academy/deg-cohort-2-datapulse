"""
checks/views.py
────────────────────────────────────────────────────────────────────────────────
TODO: define your checks views here.

Use APIView for custom logic or ModelViewSet for standard CRUD.

Remember:
    • Set permission_classes = [IsAuthenticated] on every view
    • Filter querysets by request.user (ownership check) in get_queryset()
    • Use get_object_or_404(Model, id=pk, owner=request.user) for detail views
    • Log important operations with logger = logging.getLogger(__name__)
"""

# Placeholder — no views implemented yet.
